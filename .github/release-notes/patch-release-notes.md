## ${COMPONENT_NAME} ${VERSION}

A per-component patch release of **${COMPONENT_NAME}** on the `${MINOR}` line.

|              |                   |
| ------------ | ----------------- |
| Component    | ${COMPONENT_NAME} |
| Version      | `${VERSION}`      |
| Git tag      | `${TAG}`          |
| Patch branch | `${BRANCH}`       |

Only this component moves — every other Axelix component stays on its current version. This patch is
compatible with any Axelix component that shares the same `${MINOR}` `major.minor`; see the
[compatibility policy](https://axelix.io/docs/more/compatibility-and-versioning#compatibility-between-components).

### What changed

${CHANGES}
