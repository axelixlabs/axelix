# By default this component inherits the fleet-wide axelixVersion from the root gradle.properties.
# To pin it to its own patch version on a component patch branch (e.g. 1.1.0-sbs-2), uncomment the
# line below and set the patch version. On master it stays commented out so the fleet stays in lockstep.
# axelixVersion=
